#Zad 2 WIKING

#select k.nazwa from kreatura k
#left join uczestnicy u on k.idkreatury=u.id_uczestnika
#WHERE u.id_uczestnika is NULL;

#(przyk³ad u¿ycia group_concat)
#select rodzaj, group_concat(nazwa) 
#from kreatura group by rodzaj
#(przyk³ad dzia³ania ifnull)
#select ifnull(idKreatury, 'brak nazwy kreatury') from ekwipunek order by idKreatury desc
#albo drugi sposób z "if"
#select if(idKreatury is Null, 'brak nazwy kreatury', idKreatury) from ekwipunek order by idKreatury desc
#select w.nazwa,SUM(e.ilosc) from wyprawa w
#inner join uczestnicy u on w.id_wyprawy=u.id_wyprawy
#inner join ekwipunek e on u.id_uczestnika=e.idkreatury
#group by w.id_wyprawy


#zad 2 WIKING

#select w.nazwa rodzaj, count(u.id_uczestnika) liczba, group_concat(k.nazwa) imiona
#from wyprawa w inner join uczestnicy u on w.id_wyprawy=u.id_wyprawy
#inner join kreatura k on k.idKreatury=u.id_uczestnika
#group by w.id_wyprawy

#select*from wyprawa w
#inner join kreatura k on k.idKreatury=w.kierownik
#inner join etapy_wyprawy ew on w.id_wyprawy=ew.idWyprawy
#inner join sektor s on ew.sektor=s.id_Sektora
#order by w.data_rozpoczecia,ew.kolejnosc


#zad 3 WIKING

#select s.nazwa, count(ew.sektor) from etapy_wyprawy ew
#inner join sektor s on ew.sektor=s.id_sektora
#group by s.nazwa with rollup

#select s.nazwa, count(ew.sektor) from sektor s
#left join etapy_wyprawy ew on ew.sektor=s.id_sektora
#group by s.nazwa 